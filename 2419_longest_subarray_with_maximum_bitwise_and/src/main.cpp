#include <vector>

using namespace std;

struct Solution
{
    int longestSubarray(vector<int>& nums) 
    {
        auto size = nums.size();
        int beg = 0;
        int end = 0;
        int sum = 0;
    
        for (int i = 0; i != size; ++i)
        {
                        
        }

        return end - beg;
    }
};

int main()
{

}