class Solution 
{
public:
    int minimumDeletions(const string& s)
    {
        auto size = s.size();
        for(size_t forward_index = 0, backward_index = size; forward_index != size, backward_index != 0; ++forward_index, --backward_index)
        {

        }
    }
};
